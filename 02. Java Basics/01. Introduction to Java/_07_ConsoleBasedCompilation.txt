// Using javac compile from the console the program SumTwoNumbers.java.
// Execute at the console the obtained class file SumTwoNumbers.class
// and check whether it works correctly. Submit the console commands you
// have used to compile and run the program as part of your homework
// in a text file compile-and-run-commands.txt.

Commands used:

set "PATH=%PATH%;C:\Program Files\Java\jdk1.6.0_18\bin"			// Adds a the path to the JDK to the PATH variable
javac _06_sumtwonumbers.java									// Executed from the folder containing the .java file. Compiles the source code.
java _06_SumTwoNumbers											// Executed from the folder containing the .class file. Runs the program.